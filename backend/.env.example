# Logged Fitness Tracker - Environment Configuration
#
# ============================================
# FIELDS MARKED [NEEDS FILLED] REQUIRE YOUR VALUES
# ============================================

# App Settings
APP_NAME="Logged Fitness Tracker API"
APP_VERSION="1.0.0"
DEBUG=true
DB_AUTO_INIT=true

# Server
HOST=0.0.0.0
PORT=8000

# --------------------------------------------
# Database - Neon PostgreSQL
# --------------------------------------------
# [NEEDS FILLED] Get your connection string from Neon console: https://console.neon.tech
# Format: postgresql+asyncpg://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL=postgresql+asyncpg://[NEEDS FILLED]@[NEEDS FILLED].neon.tech/logged_db?sslmode=require
DATABASE_ECHO=false

# --------------------------------------------
# User Auth Database - Neon PostgreSQL
# --------------------------------------------
# [NEEDS FILLED] Can use same Neon DB or separate one for user data
# If using same DB, copy DATABASE_URL value here
AUTH_DATABASE_URL=postgresql+asyncpg://[NEEDS FILLED]@[NEEDS FILLED].neon.tech/logged_auth_db?sslmode=require

# Redis (optional, for caching)
REDIS_URL=redis://localhost:6379/0

# --------------------------------------------
# JWT Authentication
# --------------------------------------------
# [NEEDS FILLED] Generate a secure random key (min 32 chars)
# Example: openssl rand -hex 32
JWT_SECRET_KEY=[NEEDS FILLED]
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Magic Link
MAGIC_LINK_EXPIRE_MINUTES=15
MAGIC_LINK_BASE_URL=logged://auth/verify

# Email (for magic links - optional for dev)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASSWORD=your-smtp-password
# SMTP_FROM_EMAIL=noreply@logged.app

# --------------------------------------------
# Claude API (Anthropic) - AI Features
# --------------------------------------------
# Default AI provider: "anthropic" or "openai"
AI_PROVIDER=anthropic

# [NEEDS FILLED] Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=[NEEDS FILLED]

# Claude model for text/chat
CLAUDE_MODEL=claude-sonnet-4-20250514

# Claude model for vision (food photo analysis)
# Claude Sonnet and Opus support vision natively
CLAUDE_VISION_MODEL=claude-sonnet-4-20250514

# Max tokens for AI responses
CLAUDE_MAX_TOKENS=4096

# --------------------------------------------
# OpenAI (Optional) - Alternative AI Provider
# --------------------------------------------
# OPENAI_API_KEY=your-openai-key
# OPENAI_MODEL=gpt-4-turbo-preview
# OPENAI_VISION_MODEL=gpt-4-vision-preview
# OPENAI_BASE_URL=https://api.openai.com/v1

# --------------------------------------------
# Open Food Facts
# --------------------------------------------
OFF_API_URL=https://world.openfoodfacts.org/api/v2
OFF_USER_AGENT=Logged Fitness Tracker/1.0 (contact@logged.app)

# Apple Sign In (optional)
# APPLE_CLIENT_ID=com.yourcompany.logged
# APPLE_TEAM_ID=YOUR_TEAM_ID
# APPLE_KEY_ID=YOUR_KEY_ID
# APPLE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# CORS
CORS_ORIGINS=["*"]

# ============================================
# SUMMARY OF FIELDS NEEDING YOUR VALUES:
# ============================================
# 1. DATABASE_URL - Neon connection string (from Neon console)
# 2. AUTH_DATABASE_URL - Neon connection string for auth (can be same as DATABASE_URL)
# 3. JWT_SECRET_KEY - Generate with: openssl rand -hex 32
# 4. ANTHROPIC_API_KEY - From https://console.anthropic.com/
# ============================================
